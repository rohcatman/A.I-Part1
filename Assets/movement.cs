using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class movement : MonoBehaviour
{
  
//   private float c_destroyPositionY =  0;
//   public float speed;
    // Update is called once per frame
    void Update()
    {
    //    transform.position = new Vector3(
    //        transform.position.x,
    //        transform.position.y-2.0f * Time.deltaTime, //간격과 상관이 있는듯
    //        transform.position.z
    //    );

       //measure distance between the plain and cube position

    //    if(transform.position.y <= c_destroyPositionY) {
    //        Destroy(gameObject);
    //    }
    }
}
